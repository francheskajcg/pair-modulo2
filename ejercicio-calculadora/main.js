'use strict'

const calculadoraDeCositas = (boderbox, width, padding, bodersize) => {
    if (boderbox === true) {
        console.log(width + padding + bodersize);
    }
    
    else {
        console.log(width + padding);
    }

    
}

const resultado = calculadoraDeCositas(true, 100, 20, 5)

